Personal and under development. Do not look. Do not use.

    Outside fabric:
        Create VPS
        Add maintenance user

    Once per setup:
        fab install

    Once per project:
        fab add_user
        fab project_init

    Multiple times per project:
        fab deploy
